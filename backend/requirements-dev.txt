# Development and test tooling.
#
# requirements.txt holds what the Lambda bundle needs at runtime and nothing
# else, so keep additions here unless a deployed function actually imports it.
# boto3 is not in either file: the Lambda runtime provides it, and it is
# installed here only so the tests can import the DynamoDB repositories.

-r requirements.txt

# Testing
pytest>=8.0.0
boto3>=1.34.0

# Code quality - the commands documented in README.md
ruff>=0.1.0
mypy>=1.7.0

# Deployment
aws-sam-cli>=1.99.0
